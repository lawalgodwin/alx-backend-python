pipeline {
    agent any

    stages {
        stage("Pull code from GitHub") {
            steps {
                git branch: 'main',
                    credentialsId: 'GitHub_Cred',
                    url: 'https://github.com/lawalgodwin/alx-backend-python'
            }
        }
        stage("install dependencies") {
            steps {
                dir('messaging_app') {
                    sh '''
                        rm -rf venv
                        python3 -m venv venv
                        bash -c "source venv/bin/activate"
                        bash -c "build-essential zlib1g-dev libncurses5-dev libgdbm-dev libnss3-dev libssl-dev libreadline-dev libffi-dev -y"
                        bash -c "apt install python3-dev default-libmysqlclient-dev build-essential -y"
                        venv/bin/pip install -r requirements.txt
                    '''
                }
            }
        }
        // stage("Test") {
        //     steps {
        //         sh 'pytest'
        //     }
        // }
    }
    
    post {
        always {
            echo 'pytest --cov'
        }
    }
}